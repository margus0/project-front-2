import styled from 'styled-components';

export const StyledPage = styled.div`
  button {
    position: relative;
    top: 7rem;
    left: 5vw;
  }

  p.total {
    position: absolute;
    left: 80rem;
    top: 10rem;
  }
`;
